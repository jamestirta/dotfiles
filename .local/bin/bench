#!/usr/bin/bash
if [[ "$1" =~ ^[0-9]+$ ]]; then
	num=$1
	time {
	for i in $( seq 1 "$num" ); do
		"${@:2}" > /dev/null
	done
	}
else
	num=500
	time {
	for i in $( seq 1 "$num" ); do
		"$@" > /dev/null
	done
	}
fi
